package com._520it.pss.template;

// �ɱ��ڲ������
/*
 * public class StudentResultSetHandler implements
 * IResultSetHandler<List<Student>> {
 * 
 * @Override public List<Student> handle(ResultSet rs) throws Exception {
 * List<Student> stuList = new ArrayList<Student>();
 * 
 * while (rs.next()) { Student stu = new Student(); stu.setId(rs.getLong("id"));
 * stu.setName(rs.getString("name")); stu.setAge(rs.getInt("age"));
 * stuList.add(stu); } return stuList; }
 * 
 * }
 */
